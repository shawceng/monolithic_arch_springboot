<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.fenixsoft.bookstore.domain.account.AccountRepository">
    <select id="findAll" resultType="Account">
        SELECT * FROM account;
    </select>

    <select id="findById" resultType="Account">
        SELECT * FROM account WHERE id=#{id}
    </select>

    <select id="findByUsername" resultType="Account">
        SELECT * FROM account where name=#{username}
    </select>

    <insert id="save">
        INSERT INTO account (id, username, password, name, avatar, telephone, email, location)
        VALUES (#{id}, #{username}, #{password}, #{name}, #{avatar}, #{telephone}, #{email}, #{location})
    </insert>

</mapper>